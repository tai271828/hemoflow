[pytest]
markers =
    preproc: preprocessor-level integration tests (need the lbmpre/uv venv, no solver)
    solver_quick: end-to-end solver runs, minutes per case (need build/hemoFlow)
    solver_full: end-to-end solver runs at production resolution (nightly)
addopts = -v --tb=short
